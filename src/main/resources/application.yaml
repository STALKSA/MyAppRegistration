server:
  port: 8000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/myApp
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
